<Window x:Class="SpotifyListener.LyricsDisplay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpotifyListener"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="{Binding Track}" Height="560" Width="350">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="58*"/>
            <ColumnDefinition Width="113*"/>
        </Grid.ColumnDefinitions>
        <RichTextBox x:Name="txt_Lyrics" HorizontalAlignment="Left" Height="529" VerticalAlignment="Top" Width="342" Grid.ColumnSpan="2" IsReadOnly="True">
            <FlowDocument>
                <Paragraph>
                    <Run Text="{Binding Lyrics,Mode=OneWay}"/>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>

    </Grid>
</Window>
